abbr -a o xdg-open
abbr -a v nvim
abbr -a cp cp -v
abbr -a mv mv -v
abbr -a mkd mkdir -pv
abbr -a chx chmod +x
abbr -a dv doas nvim
abbr -a dcp doas cp -v
abbr -a dmv doas mv -v
abbr -a dmkd doas mkdir -pv

abbr -a ls exa --color always --group-directories-first
abbr -a la exa -al --color always --group-directories-first --git
abbr -a ll exa -l --color always --group-directories-first --git -ahl
abbr -a l  exa -a | rg ^\.

abbr -a xs doas xbps-install -S
abbr -a xr doas xbps-remove -Ro
abbr -a xu doas xbps-install -Su
abbr -a xq xbps-query -Rs
abbr -a xbi xbps-install
abbr -a xf xbps-fetch
abbr -a q xbps-query

abbr -a z zathura
abbr -a gc git clone
abbr -a sx startx
abbr -a ns nsxiv
abbr -a bm bashmount
abbr -a sv doas vsv
abbr -a rr ranger
abbr -a pa paru

abbr -a ea nvim ~/.config/shell/aliases
abbr -a ef nvim ~/.config/fish/config.fish
abbr -a ex nvim ~/.config/sxhkd/sxhkdrc
abbr -a ep nvim ~/.config/shell/profile

abbr -a frc source ~/.config/fish/config.fish
abbr -a resx pkill -USR1 -x
abbr -a rm trash-put

abbr -a sys systemctl
abbr -a dsys doas systemctl
